name: G5-C Apply and Validate

on:
  push:
    branches:
      - feature/m0-g5-rupture-vent

permissions:
  contents: write

jobs:
  apply-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: windows-latest
    steps:
      - name: Checkout G5-C branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply G5-C implementation
        run: python tools/g5c_apply.py

      - name: Format Rust
        run: cargo fmt --all

      - name: Core regression
        run: cargo test -p powdergame-core

      - name: Parse all production WGSL
        run: cargo test -p powdergame-gpu --test wgsl_parse -- --nocapture

      - name: Compile workspace all targets
        run: cargo check --workspace --all-targets

      - name: Diff sanity
        shell: bash
        run: git diff --check

      - name: Remove temporary validation helpers
        shell: bash
        run: git rm tools/g5c_apply.py .github/workflows/g5c-apply-and-validate.yml

      - name: Commit validated G5-C implementation
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: implement G5-C structural rupture and vent opening"
          git push origin HEAD:feature/m0-g5-rupture-vent
