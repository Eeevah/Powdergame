[package]
name = "powdergame-scenarios"
description = "Shared deterministic staging for Powdergame benchmark and regression scenarios."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
powdergame-core = { workspace = true }
powdergame-gpu = { workspace = true }
wgpu = { workspace = true }

[dev-dependencies]
pollster = { workspace = true }
