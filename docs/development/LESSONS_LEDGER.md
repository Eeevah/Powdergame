# Powdergame Lessons Ledger

이 문서는 **승격된 교훈만** 보존하는 append-only 원장이다. 기존 항목의 의미를 조용히 바꾸지 않는다. 결론이 바뀌면 새 ID를 추가하고 `Supersedes`를 기록한다.

| ID | Date | Status | Observation / loss | Adopted rule | Machine guard / durable location | Evidence |
|---|---|---|---|---|---|---|
| PG-L001 | 2026-08-17 | adopted | Warm Rust build는 약 0.3초였지만 직렬 workspace test는 약 310.84초였다. “빌드가 느리다”는 표현이 실제 병목을 가렸다. | 변경 영향에 따라 FULL을 조건부로 실행하고 최종 위험 경계에서 한 번만 수행한다. | `VALIDATION_POLICY.md`, `config/development-policy.json`, `tools/dev.ps1 validation-plan` | `build-audit-20260817T1115497421092Z-5bb4e6fb`, source `5af031f1...` |
| PG-L002 | 2026-08-17 | adopted | Gate/worktree마다 Cargo `target`이 2–8GB씩 중복되어 약 45GB까지 증가했다. | active worktree cache는 유지하고 retired worktree만 `cargo clean → remove`한다. | `WORKTREE_ARTIFACT_EXECUTABLE_POLICY.md`, `tools/dev.ps1 audit` | 사용자 디스크 inventory, Build-Time Audit target 5.60GiB |
| PG-L003 | 2026-08-17 | adopted | 동일 앱을 여는 Gate별 BAT와 오래된 EXE 위치가 사용자를 혼란스럽게 했다. | 사용자용 app binary 1개, 일반 launcher 1개, experiment launcher 1개만 유지한다. | `config/development-policy.json`, repository audit; legacy wrappers는 G8-B closure까지 명시적 allowlist | 사용자 결정 |
| PG-L004 | 2026-08-17 | adopted | FULL 진행 중 새 요구가 들어와 이미 진행한 장시간 검증을 중단하고 새 SHA에서 다시 시작했다. | requirement freeze 뒤 source seal/FULL을 시작하고, 비차단 follow-up은 현재 checkpoint 뒤로 미룬다. | `VALIDATION_POLICY.md` source-seal contract | Fire/Heat 작업 로그 |
| PG-L005 | 2026-08-17 | adopted | docs-only closure 후에도 FULL을 다시 실행하려는 습관이 반복됐다. | tested source SHA와 docs closure SHA를 분리하고 docs-only 변경에는 Rust/GPU FULL을 실행하지 않는다. | validation class `docs-only` | Sand/Water docs closure workflow |
| PG-L006 | 2026-08-17 | adopted | Candidate, smoke, FULL이 서로 다른 증거 역할인데 같은 correctness 검사를 반복했다. | targeted error paths, minimal smoke, candidate, FULL의 역할을 분리하고 서로 대체하지 않는다. | `VALIDATION_POLICY.md` role table | G8-B Sand/Water/Fire review |
| PG-L007 | 2026-08-17 | adopted | 사용자가 매번 장기 실행을 기다리고 수동 screenshot/HUD transcription을 해야 했다. | Harness가 keyframe·telemetry·report를 만들고 사람은 `NEEDS_HUMAN_REVIEW` 장면만 추가 관찰한다. | Experiment Harness, Contact Sheet, Review Packet | Sand pilot / Water candidate |
| PG-L008 | 2026-08-17 | adopted | 자동 verdict나 Review Packet 하나를 더 넓은 Gate 승인 또는 forensic proof로 오해할 위험이 있었다. | 자동 판정, 사용자 승인, forensic audit bundle의 범위를 분리한다. | evidence contract와 review prompt boundary | Sand Review Prompt and adversarial review |
| PG-L009 | 2026-08-17 | adopted | 첫 policy audit 구현에서 PowerShell 함수명이 `Git` 실행파일을 가렸고, 다음 수정에서는 자동 변수 `$Args`와 같은 이름을 써 Git argument가 비어 두 차례 CI가 실패했다. | 외부 명령 wrapper는 command 이름·PowerShell 자동 변수와 겹치지 않게 하고, 실행파일을 하나 선택한 뒤 named array parameter로 전달한다. 새 guard는 실제 CI에서 PASS하기 전 채택하지 않는다. | `tools/dev.ps1`의 `Invoke-RepoGit -GitArgs`, GitHub Actions `Development policy audit` | failed runs `32036149706`, `32036356485`, `32036736080`; passing run `32036953896` |
| PG-L010 | 2026-08-18 | adopted | Contact Sheet가 milestone 종류 순서로 생성되면 실제 simulation 시간축이 뒤섞이고 같은 tick의 상태가 중복 타일로 보일 수 있었다. | Frame은 `sim_tick` 오름차순으로 정렬하고, 같은 tick과 state hash의 milestone은 가능한 한 한 frame의 badge로 접으며, reset은 항상 마지막에 둔다. | Contact Sheet chronological ordering / same-tick badge folding / reset-last deterministic ordering test | Fire / Heat candidate review follow-up |
| PG-L011 | 2026-08-18 | adopted | 원본 Run `HASHES.sha256`와 Audit Bundle 내부 경로가 다르고 canonical Git archive의 EOL 표현이 실제 build-input bytes와 다를 수 있어 bundle 하나만으로 forensic verification 경계가 모호했다. | `SOURCE_INPUT_MANIFEST.json`이 해시한 exact build-input bytes와 canonical Git archive를 구분하고, Audit Bundle 자체 inclusion/path/omission contract를 별도로 기록한다. | Bundle-local manifest/path mapping/hash tests와 exact source-input-bytes archive byte-identity test | Fire / Heat Audit Bundle review follow-up |
| PG-L012 | 2026-08-18 | adopted | Pressure through opening milestone은 검출됐지만 같은 결과를 Wood combustion이 만들 수 있었고, complete opening tick이 900-active-tick burn duration과 맞물렸다. | Scenario acceptance는 결과 milestone만이 아니라 이름을 붙인 causal chain을 증명하고 같은 결과를 만드는 다른 subsystem을 배제해야 한다. | Subsystem-specific confound telemetry, causal predicates, and synthetic confound regressions | Pressure run `g8b-pressure-burst-v0-20260818T014452058676Z-353fb706` human review |
| PG-L013 | 2026-08-18 | adopted | UTC timestamp를 local/KST로 다시 해석해 task wall time이 약 9시간 부풀었다. | Duration은 RFC3339 UTC 또는 monotonic counter로만 계산하고, timezone은 사람용 표시에만 쓴다. | Fixed-clock, UTC/KST double-offset, cross-process, and UTC-versus-monotonic consistency tests in `tools/test-dev-timer.ps1` | Timer fix source `328757bd8cd5b07cd0ed4c66a592f973dcd66981` |
| PG-L014 | 2026-08-18 | adopted | 선행 task의 wall `6,473` 초 중 command 기록은 `126` 초에 불과해 command timing만으로 병목과 작업 비용을 설명할 수 없었다. | Command timing과 함께 phase union, wall 대비 비율, 그 둘로 설명되지 않는 unclassified time을 출판한다. | Session summary command/phase union, ratios, unclassified bucket, and overlap regressions | Timer fix source `328757bd8cd5b07cd0ed4c66a592f973dcd66981` |
| PG-L015 | 2026-08-18 | adopted | 명시적 `--benchmark-gallery` smoke는 통과했지만 사용자가 실제로 실행하는 no-argument canonical path는 G0 empty clear 화면을 열었다. Repository 밖 convenience launcher는 canonical audit 범위에서 빠질 수도 있었다. | User-facing launcher acceptance는 exact no-argument entrypoint를 실행해 검증하고 technical baseline은 explicit diagnostic flag로만 제공한다. 외부 convenience launcher는 최종 migration 전까지 내용을 inventory한다. | No-argument mode routing unit test, no-argument bounded launcher smoke with Gallery staging/log assertion, explicit runtime-baseline regression, and `tools/dev.ps1 audit` launcher contract | Pressure Burst acceptance launcher follow-up |
| PG-L016 | 2026-08-18 | adopted | Required SHA guard가 exact fetch 전의 stale local remote-tracking ref만 비교해, 실제 remote에 docs commit `d684221...`가 있었지만 local `ba75ede...`에서 ahead/behind `0/0`으로 오판하고 작업을 중단했다. | Remote branch의 required SHA를 전제로 할 때는 exact branch fetch → `origin/<branch>` SHA 검증 → clean 확인 → ff-only 동기화 → final `0/0` 확인 순서를 지킨다. Stale local ahead/behind `0/0`만으로 live remote equality를 주장하지 않는다. | Preflight ordering contract and exact fetched-origin SHA guard | Stale local `ba75ede72614ae5caea9891345c4052901c41d29`; fetched remote `d6842215c2417d66c34914ba165e52677b7c89de`; preserved aborted session `20260818T121852227Z-cell-inspector-v0-ba75ede7` |
| PG-L017 | 2026-08-18 | adopted | Cell의 색과 복잡한 diagnostic HUD만으로는 사용자가 Matter identity와 현재 field 상태를 즉시 이해하기 어렵고, GPU sample의 시점을 명시하지 않으면 stale 값을 현재 상태로 오해할 수 있다. | Compact hover로 canonical Material identity를 직접 표시하고, detailed Inspector는 field 값과 sample simulation tick / diagnostic sequence / freshness를 함께 표시한다. | Letterbox/DPI coordinate mapping, canonical Material-name/invalid-ID, sample epoch/identity, reset·scenario-switch stale invalidation, and bounded readback-cost tests | Cell Inspector v0 source `a2aac71219e145b84d6dd050865a81631da490bd` |
| PG-L018 | 2026-08-18 | adopted | 짧게 표시되는 `Collecting...`은 의미 있는 정보를 전달하기보다 hover 전환 때 UI flicker처럼 느껴졌다. | 빠른 비동기 UI의 정상 pending 상태는 기본적으로 silent하게 유지하고, fresh data가 준비된 순간에만 사용자 정보를 표시한다. 빈 시간을 이전 target의 stale data로 채우지 않는다. | Pending-hidden, stale-hidden, reset/switch-hidden, ready-visible presentation-state tests | Cell Inspector silent-pending source `3c342d25099683df53e303d1920cebe1f6578b74` |
| PG-L019 | 2026-08-19 | adopted | 테스트 fixture가 coordinator 내부 이름 `wall_ms_per_tick`을 사용해 통과했지만, 실제 historical producer는 `wall_per_tick`을 출력해 end-to-end aggregation이 실패했다. | Consumer test는 내부 model 이름이 아니라 실제 producer의 canonical schema와 vocabulary를 최소 한 사례 포함해야 하며, 외부 schema와 내부 field 이름은 명시적 adapter에서 분리한다. | Actual-producer-schema fixture, strict unit validation, missing/duplicate/internal-alias rejection, and aggregation-only replay before official capture | Failed replacement Pilot `g8c-pilot-8ee1ae238c32-6341f4f59218`; passing replay `g8c-aggregation-replay-20260819T015515996891Z-fc408076b67a` |
| PG-L020 | 2026-08-19 | adopted | 첫 playable edit path가 한 compute bind group에 아홉 storage buffer를 묶자 실제 adapter limit 8에서 pipeline creation이 실패했다. CPU-side world mirror나 부분 write로 우회하면 authority와 Current/Next atomicity가 깨질 위험이 있었다. | Interactive edit는 bounded/coalesced command buffer와 하나의 submission을 유지하되, hardware binding limit 안의 ordered compute passes로 field write와 flags hygiene를 분리한다. Rendering/picking/Inspector도 동일 camera transform을 공유한다. | Actual GPU edit/reset integration test, storage-binding-budget structure, Current/Next/flags/wake-halo assertions, shared-transform viewport tests | G9-A source `f9a7087249bf6ffa0b6d47ad7568ba1798f591a3` |
| PG-L021 | 2026-08-22 | adopted | TE-2 후보는 F/N/I와 수치를 광고했지만 실제 입력·관측 경로가 빠졌고, TE-3 초기 Scene 2–4는 label이 주장한 조건을 staging하지 않았다. 자동 물리 PASS가 user-review surface의 truth를 보장하지 못해 사용자 재작업이 반복됐다. | 모든 review scene은 `claim → staged state → authoritative metric → bounded checkpoint`를 하나의 계약으로 갖고, 후보 수준 semantic test가 이를 실행해야 한다. | Candidate semantic fixtures, generation-safe fixed rows, fresh/sample tick checks, control routing regression | TE-2 remediation `097728128343...`; TE-3 candidate `c2f4f2bb16b0...`, docs `f1f1a8532fab...` |
| PG-L022 | 2026-08-22 | adopted | TE-3Q one-shot proof의 named fixture 일부는 literal 문자열·상수·축소 예제만 반환했지만 `14/14 PASS`처럼 보였다. 이름이 실제 production-equivalent path 실행을 대신했다. | Named fixture는 required operation/path counter가 0이 아님을 증명해야 하며, 실행하지 않은 의무는 PASS가 아니라 `NOT_ESTABLISHED`로 기록한다. | Fixture coverage manifest, path counters, label-only PASS rejection, fresh independent review | ADR-0011 and `TE3Q_CONSERVATIVE_PHASE_PACKETS_DESIGN.md`, source `b5a3405bffba...` |
| PG-L023 | 2026-08-22 | adopted | TE-5X one-shot evidence가 dependency/version guard에서 끝나 `1 attempt / 0 completion`이었고 후보 A/B/C는 평가되지 않았다. evidence run 안에 syntax/dependency preflight가 섞여 복구도 재실행도 금지됐다. | One-shot evidence는 freeze 전에 non-evidence syntax/import/version/fixture-list preflight를 끝내고, receipt에서 attempt와 completion을 분리한다. 완료 0이면 모델 결과도 0이다. | Preflight-only command, immutable script hash after preflight, receipt fields `attempts/completions/evaluated_candidates` | TE-5X final `df6801272aa7...`, D-022 |
| PG-L024 | 2026-08-22 | adopted | TE-5B의 same-tick exclusive EMPTY token은 한 Tick의 중복 claim만 막고 Steam 이동으로 vacancy가 다음 Tick에 새 capacity처럼 재사용되는 것을 막지 못했다. | Tick을 가로질러 소비되는 자원은 한 arbitration epoch로 증명하지 않는다. Persistent conservation state 또는 그 자원을 직접 표현하는 ontology가 필요하다. | Multi-tick vacancy-walk adversarial fixture, cross-tick accounting invariant | blocked ADR-0007, source `6a1c83fad702...` |
| PG-L025 | 2026-08-22 | adopted | TE-5C의 local proportional sharing은 완전한 capacity 배정이 존재하는 비대칭 배치에서도 사용 가능한 EMPTY를 버리고 false pressure 100을 만들었다. | Local heuristic은 global feasibility의 증명이 아니다. 거짓 실패가 제품 consequence를 만들 수 있으면 exact oracle 또는 completeness counterexample가 필요하다. | Asymmetric allocation fixture, exact small oracle, false-pressure rejection | blocked ADR-0008, source `94e4da7603da...` |
| PG-L026 | 2026-08-22 | adopted | TE-5D의 `MAX_REASSIGNMENT_DEPTH=6`은 길이 8 augmenting path가 있는 합법 상태에서 matching을 못 찾아 false rupture pressure를 만들었다. 깊이 상수를 키우면 반례만 뒤로 밀린다. | Fixed search depth를 correctness guarantee로 쓰지 않는다. Arbitrary-length counterexample를 닫거나 불완전성을 제품 의미로 명시한다. | Augmenting-chain family 8/16/64, exact small matching oracle, solver-delay pressure prohibition | blocked ADR-0009, source `f5b146571f2c...` |
| PG-L027 | 2026-08-22 | adopted | 수학 reference model은 quantity/H를 보존했지만 movement, Environment receiver, Void, chunk/sleep, editor/reset, Current/Next transaction을 생략해 production evidence로 승격할 수 없었다. | Reference PASS는 모델링한 경계만 증명한다. 결과 JSON과 보고서는 `MATHEMATICAL`, `GPU UNKNOWN`, `PRODUCT UNKNOWN`, `NOT_ESTABLISHED`를 분리한다. | Coverage matrix, production-path inventory, independent static review, source-bound evidence labels | TE-3Q reference and review, source `b5a3405bffba...` |
| PG-L028 | 2026-08-22 | adopted | 역사적 G5 Water expansion-pressure chain을 TE-3와 원자적으로 보존하려다 token, matching, component, packet 설계가 연속 차단되어 사용자가 원한 자연스러운 Water/Steam cycle이 지연됐다. | Historical evidence는 source-bound로 보존하되 과거 구현을 현재 product invariant로 자동 승격하지 않는다. 현재 사용자 목표를 독립 Gate로 닫고 deferred regression을 명시한다. | Decision ledger amendment D-024, historical evidence labels, active-source regression inventory | TE-3 runtime `41467219819c...`; G5 evidence historical |
| PG-L029 | 2026-08-22 | adopted | Steam이 100°C latent plateau에서 phase energy를 잃고 있어도 Sandbox Inspector가 phase energy를 읽지 않아 사용자는 “영원히 같은 온도”로 보았다. 올바른 hidden truth도 관측 불가하면 제품에서는 고장처럼 보인다. | 사용자가 판정해야 하는 authoritative progress는 bounded Inspector/HUD에 노출하고, 도구 이름은 보이는 결과가 아니라 작업을 설명한다. | Mode-aware 24-byte Inspector profile, phase-progress formatter, `Add Heat/Remove Heat`, sealed-boundary disclosure | TE-3 user screenshots; runtime `4146721...`; candidate `c2f4f2b...` |
| PG-L030 | 2026-08-22 | adopted | canonical EXE가 생성됐지만 TE-3 candidate는 CLI argument 없이는 열리지 않아 사용자가 직접 command를 입력해야 했다. Artifact 존재와 usable review entrypoint를 혼동했다. | User-review deliverable은 exact one-click path, target/arguments, working directory와 실제 launch를 함께 검증한다. 단일 EXE 정책은 유지한다. | Launcher route audit, local shortcut/entrypoint verification, final report artifact contract | EXE `F15B8B119844...`; `run_powdergame.bat phase-cycle` |
| PG-L031 | 2026-08-22 | adopted | personal-infra-wiki 로컬 checkout의 사용자 변경을 보호한 것은 맞지만, 이를 이유로 verified lessons의 Wiki 승격을 여러 세션 미뤘다. 안전 보존 규칙이 자산 축적 중단으로 변했다. | Dirty local Wiki는 건드리지 않되 verified `origin/main`에서 clean remote branch/PR 또는 temporary worktree로 승격한다. 보호와 축적을 동시에 만족시킨다. | `wiki-dirty-worktree-preservation` remote-promotion path, PR base SHA, untouched-path report | Wiki base `b8c22c1dc477...`; 사용자 지시 2026-08-22 |
| PG-L032 | 2026-08-22 | adopted | PG-L020 이후 TE-2/3/5의 값비싼 반례가 ADR·review·checkpoint에만 흩어지고 Lessons Ledger와 Wiki로 승격되지 않았다. 기록을 많이 남기는 것과 재사용 가능한 자산을 만드는 것을 혼동했다. | Substantial task closeout에 `LESSON_PROMOTION: REQUIRED / PROJECT_ONLY / NONE`를 강제하고 root cause, failed guard, adopted rule, machine guard, Wiki route를 보고한다. | `AGENTS.md`, `DEVELOPMENT_LEARNING_LOOP.md`, personal-infra-wiki Lesson Promotion Gate | 사용자 직접 결정 2026-08-22 |
| PG-L033 | 2026-08-22 | adopted | TE-3 Scene 1은 Water heating → Steam rise → condensation → Water return을 광고했지만 3,885 Water와 유한한 64-Cell `800 C` Stone 열원이 약 `85.824 C`에서 평형에 도달해 tick `21,160`에도 Steam `0` / phase energy `0`이었다. 초기 geometry·온도 검사와 전이 전 bounded launch가 전체 finite-energy causal horizon을 대신해 사용자가 장시간 실행한 뒤에야 불가능한 staging을 발견했다. | 유한 source로 causal chain을 주장하는 review scene은 source energy budget과 predeclared bounded horizon 안에서 `progress → output → transport → reverse transition → terminal/return`을 실제 production ticks로 증명한다. 하나라도 없으면 candidate는 `INVALID / NOT_ESTABLISHED`다. PG-L021을 대체하지 않고 energy-budget·전체-horizon 규칙으로 구체화한다. | Actual-Simulation milestone contract: boiling progress, first Steam, rise, condensation, returned Water, return motion, phase-family expected/min/max invariant | Original failure: tick `21160`, Water `3885`, Steam `0`, surface `85.824 C / E=0`; remediation `e9f4a3744ea3bdab0fd70f0f78aa27cb7e9fa448`: ticks `72/320/320/336/552/584`, family `288` exact |
| PG-L034 | 2026-08-22 | adopted | TE-4D one-shot은 syntax/import/fixture/config preflight를 통과했지만, 동등 metric 후보의 lexicographic tie-break가 사전 선언 계수와 다른 Oil bucket width를 고른 뒤 frozen assertion에서 종료했다. 100,000 sequence와 10,000 grid는 0회 실행됐다. | One-shot coefficient proof는 randomized model 전에 selection identity와 tie policy를 preflight 출력으로 고정하고, 동등 metric 후보가 있으면 명시적 secondary objective 또는 ambiguity failure를 선언한다. 선택 결과를 처음 확인하는 assertion을 evidence body 안에 두지 않는다. | Preflight `selection-manifest` with candidate count, equal-score set, secondary-order rule and exact selected tuple; evidence receipt separates attempt/completion/evaluated counts | D-028 frozen script `886fe5b7...`, failure receipt `6342bad5...` |
| PG-L035 | 2026-08-22 | adopted | TE-4D v2는 tick 시작의 유일한 positive-Air EMPTY face로 combustion을 허용한 뒤 같은 tick Smoke commit이 그 face를 점유해 Air access를 없앨 수 있었다. 초기 predicate PASS를 transaction 전체 invariant로 오인해 “Air loss tick에는 emission zero” 계약과 42-pass projection이 동시에 성립하지 않았다. | Consequence를 허가하는 precondition은 downstream commit이 같은 tick에 그 precondition을 무효화할 수 있는지 끝까지 감사한다. 의미를 start-snapshot으로 고정하거나, 자원을 보호하거나, commit을 원자적으로 취소하는 세 선택 중 하나를 사전에 결정한다. | Same-tick precondition-invalidation fixture: sole qualifying resource consumed/occupied by the authorized consequence; start/end snapshots, emission and rollback counters; pass-order/live-range audit | D-029 v2 review H-002; `TE4_IGNITION_KINETICS_DESIGN_V2.md`; v2 manifest `9b763c1c...53ba` |
| PG-L036 | 2026-08-23 | adopted | TE-4I Scene 4가 source fuel `0 -> 1`, Air `YES -> NO`, burning `YES -> NO`를 보여 테스트도 통과했지만, 사용자는 실제 `(209,110)` Smoke Matter를 볼 수 없었다. Candidate test가 source 부작용을 target 생성의 대리 증거로 사용했고, fitted camera에서는 한 Cell target이 오른쪽 진단 카드 아래에 놓였다. | 생성·이동 transaction을 주장하는 review surface는 source 변화가 아니라 authoritative target identity와 receiver settle을 직접 sample/display해야 한다. 시각 marker는 그 target identity에서만 파생되고 identity가 사라지면 함께 사라져야 한다. | Exact candidate Tick 0/1/2 target Material·Smoke count·Matter/Air Current/Next test, fixed target/receiver rows, target-identity marker predicate, card-avoidance camera test | User observation at `4ae570d...`; remediation `a7622bf...`; production F08 unchanged |
| PG-L037 | 2026-08-23 | adopted | TE-5R0은 모든 projected pass를 storage binding 8개 이하로 맞췄지만, pressure pass 시점에는 필요한 pre-transition phase-context와 fresh generic-impulse identity가 이미 사라졌고 OR-reduced legacy activity는 새 exact-update pass가 clear할 수 없었다. 숫자상 pass/binding budget을 semantic feasibility로 오인했다. | GPU pass 설계의 feasibility는 binding 개수뿐 아니라 각 normative input의 writer→consumer live range, event freshness/transaction order, shared reduction의 set/clear ownership을 함께 증명해야 한다. 필요한 값이 consumer 시점에 없거나 기존 producer가 반대 bit를 계속 세우면 projection은 `DESIGN BLOCKED`다. | Source-bound lifetime table with writer/overwrite/consumer rows, fresh-event witness, exact-equilibrium activity witness, and independent static review before implementation | D-035 outcome; `ADR-0013`; `TE5R_PRESSURE_VACUUM_REENTRY_DESIGN.md` High H-001/H-002/H-003 |

## Entry template

새 항목은 아래 형식으로 추가한다.

```markdown
| PG-L### | YYYY-MM-DD | proposed/adopted/superseded |
Observation and measured loss/risk |
Adopted rule |
Machine guard or durable document |
Evidence IDs / SHAs |
```
