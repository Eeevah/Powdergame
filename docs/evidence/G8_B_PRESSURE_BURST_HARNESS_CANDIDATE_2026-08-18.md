# G8-B Pressure Burst Harness Candidate

Date: 2026-08-18
Branch: `feature/m0-g8b-scenario-suite`
Pressure start SHA: `941a888c1d7398a9bb1423ecc26a2acd626f7177`
Causal remediation start SHA: `28e4e0bf7736fb95f0a9d7797cc3ab4274b91442`
Status: **CLEAN-SOURCE CANDIDATE AUTOMATIC NEEDS_HUMAN_REVIEW UNCHANGED / USER ACCEPTED WITH KNOWN FOLLOW-UP / G8-B NOT CLOSED**

## Scope

This record preserves the rejected/confounded evidence, the causal-remediation scratch, and the one clean-source immutable candidate used for Scenario 4 user acceptance. The remediation did not change production Pressure propagation, rupture thresholds, Wood properties, Temperature/phase rules, Sleep/Wake, shared staging/reset, or authored fixture geometry/Pressure; only the authored top-seam temperature was removed. This closure does not start Heavy Mixed World or G8-C.

Fire / Heat remains an immutable, separately tested and accepted scenario. Its tested source is `1635fdb9f562192123c92846e137b125c684ede9`; its run, Review Packet, Audit Bundle, and Receipt are not regenerated by this work.

## Immutable rejected scratch

- Run ID: `g8b-pressure-burst-v0-scratch-20260818T012814546855Z-998a34d6`
- Source SHA: `c7e9b75a9d186eff43e4b25dbea05b86d0f2c5bd`
- Run mode: `scratch`
- Automatic verdict: `FAIL`
- Primary classification: `HARNESS DETECTION ISSUE`
- Secondary classification: `INSUFFICIENT EVIDENCE`
- Fixture representativeness issue established: **NO**
- Production physics defect established: **NO**
- Candidate generated from this scratch/source: **NO**

The original detector counted every non-Wood seam cell as an opening. At tick 1 the top/bottom raw non-Wood counts were exactly `48 / 24`, one full-width inner layer of each eight-cell-thick seam, while `336 / 168` Wood cells still occupied the other seven layers. That damage did not establish a cavity-to-exterior path. Nevertheless, the old aggregate detector started its streak at tick 1, declared a persistent opening at tick 8, and ended the run at tick 188 after the configured 180-tick post-opening window.

Raw seam Steam first appeared at tick 2 and remained inside the authored seam (`48` initially, `54` terminal), while outside-chamber Steam was `0` throughout. The terminal 64-sample chamber mean Pressure decreased from `203.553402723` to `191.869794642` with slope `-0.186909780` per sample; chamber max ended at `283.388519287` and the mean/max runaway predicates were false. These samples describe a bounded pre-vent pressure tail, not post-vent relief, because no complete passable lane was demonstrated. Invalid material and non-finite occurrences were `0 / 0`, and exact reset was true.

The scratch artifact is immutable rejected evidence. It is not modified, overwritten, repackaged, or reclassified as a candidate. Because the false opening confirmation prematurely selected the lifecycle endpoint, the run cannot determine whether the unchanged fixture would eventually form a through channel or whether production venting would then occur.

## Immutable causally confounded candidate

- Run ID: `g8b-pressure-burst-v0-20260818T014452058676Z-353fb706`
- Source SHA: `28e4e0bf7736fb95f0a9d7797cc3ab4274b91442`
- Automatic verdict: `NEEDS_HUMAN_REVIEW` (unchanged)
- Human verdict: `FIX REQUIRED`
- Classification: `fixture_causality_confounded_by_combustion`
- Production physics defect established: **NO**
- Pressure Burst user acceptance: **NO**

This candidate correctly demonstrated Pressure activity, Wood damage, a topology-aware persistent through opening, exterior Steam, decreasing chamber mean Pressure after opening, no runaway, Water+Steam conservation, invalid/non-finite `0 / 0`, and exact reset. The artifact and its Run ID, Receipt, Review Packet, and Audit Bundle remain immutable and no retrospective automatic `PASS` is assigned.

The acceptance blocker is causal attribution. The authored top Wood seam started at `95 C`, above the Wood ignition threshold `90 C`, and Wood burn duration is `900` active ticks. At tick `896`, top Wood was `318`, top through-open lanes were `0`, Smoke was `3,682`, and Reaction-active cells were `4,000`. At tick `904`, top Wood reached `0`, all `48` top through-open lanes appeared, Smoke was `3,541`, and Reaction-active cells were `3,541`. The complete opening therefore coincided with finite combustion consumption and did not isolate the required chain `Pressure -> structural rupture -> opening -> vent`.

Primary remediation changes only the authored top-seam temperature to a value below ignition while retaining seam geometry, chamber Water/Steam, chamber temperature and Pressure, Stone, bottom seam, rupture threshold, production pass order, Sleep/Wake, and all production physics. Pressure telemetry/analysis v1 adds seam combustion, flame-event, fuel-progress, and adjacent-Pressure evidence plus the hard causal predicate `pressure_opening_precedes_combustion`. The candidate becomes superseded only when a fresh unique candidate is published; until then it remains preserved review evidence.

The remediation contract keeps manifest and frames at v0, while telemetry/events, analysis, report, and receipt use Pressure v1 because their exact causal fields and summary shape changed. A remediation scratch with pre-opening seam combustion publishes the distinct automatic outcome `FIXTURE_CAUSALITY_CONFOUNDED` and machine classification `fixture_causality_confounded`; it is not collapsed into generic `FAIL` or `NEEDS_HUMAN_REVIEW`. A complete scratch is candidate-eligible only when the causal classification is `pressure_opening_precedes_combustion` and no hard predicate fails. Candidate-mode post-processing enforces that boundary before screenshots, report, hashes, Receipt, or the sibling Audit Bundle are published; a rejected attempt remains an incomplete unique run.

## Causal remediation scratch — candidate eligible

- Run ID: `g8b-pressure-burst-v0-scratch-20260818T100203210143Z-a37e998c`
- Source HEAD: `328757bd8cd5b07cd0ed4c66a592f973dcd66981`
- Source state / run mode: exact tracked dirty build inputs / `scratch` (`git_state=dirty`)
- Exact source-input bytes seal: `SOURCE_INPUT_MANIFEST.json` SHA-256 `485c1a915afca65757c9994d0036381a92cc40d2134e293f617ead024edc966b`
- Frozen binary SHA-256: `a419aa5273009787d265db6bd444126589890a82d7839b82a9c31ed56918303a`
- Automatic verdict: `NEEDS_HUMAN_REVIEW` (review-only)
- Causal classification: `pressure_opening_precedes_combustion`
- Hard predicates: **10 / 10 PASS**
- Candidate blocker / failed hard predicates: `false` / `[]`
- Candidate eligibility: **YES, after the same remediation is sealed as a clean source**

Pressure activity and initial Wood damage first appeared at tick `1`. The first topology-aware rupture/through opening started at tick `7,216` (sample `904`) and was confirmed after three diagnostics at tick `7,232` (sample `906`). Raw outside-chamber Steam first appeared with one cell at tick `7,216`; the confirmation-gated causal exterior-Steam milestone is tick `7,232`. First post-opening chamber relief followed at tick `7,233`. At first opening, `73` adjacent pressure-medium cells reached max Pressure `83.301017761`, above the Wood rupture threshold `80`. No reseal was observed.

The causal confound guard is clean: first seam combustion and first seam fuel progress are both `None`; combusting cells, flame-event cells, fuel-progress sum, and fuel-progress max all remained `0` through opening confirmation. Thus the opening was observed before any seam combustion/fuel path could produce it.

Chamber mean/max Pressure fell from the pre-opening peaks `223.582887701 / 319.461242676` to the post-opening reference `61.290115379 / 148.151290894`, then to terminal `53.460612654 / 145.603897095`. The 64-sample terminal mean declined `56.547848156 -> 53.460612654` with slope `-0.048798238` per sample and no positive mean/max steps, so runaway was false. Exterior Steam peak/final was `99 / 99`; invalid material/non-finite occurrences were `0 / 0`; exact reset was true.

The automatic result remains `NEEDS_HUMAN_REVIEW` only because one seam ruptured, Pressure activity and a vent plume remained, and terminal activity was nonzero. Those are explicitly review-only conditions, not causal or hard-predicate failures. This scratch does not supersede the immutable human-rejected candidate and is not itself user acceptance or a clean-source candidate.

## Clean-source immutable candidate and human disposition

- Run ID: `g8b-pressure-burst-v0-20260818T101046792957Z-17158748`
- Source SHA: `43e19d0f3b43aa0c15bf31e98f6401ba5f885170`
- Automatic verdict: `NEEDS_HUMAN_REVIEW` (**unchanged**)
- Human verdict: **USER ACCEPTED WITH KNOWN FOLLOW-UP**
- Causal predicate: `pressure_opening_precedes_combustion` — **PASS**
- Review Packet SHA-256: `f1523a816e5c97ad15fbd53e910553f8bbe59c3c7b78cf7fd6089f7dc143c0eb`
- Audit Bundle SHA-256: `6f8e4579afb7b3375e571b1481b46d95e0e8533b4e718e6c5bd3ca4d120e5bc8`
- Receipt SHA-256: `c21374d5bef5e3d2233cc488c893829b5285988887bcbd35db00f3ae87f72a64`

The candidate reproduced the required named causal chain without the combustion confound. Relief-seam combusting cells, flame-event cells, fuel-progress sum, and fuel-progress max remained `0` through persistent-opening confirmation. The first through opening was tick `7,216`; persistent-opening confirmation and causal exterior Steam were tick `7,232`. Opening-adjacent Pressure reached `83.301017761`, above the Wood rupture threshold `80`.

Terminal chamber mean/max Pressure was `53.460612654 / 145.603897095`. The terminal mean window decreased from `56.547848156` to `53.460612654`, runaway was `false`, invalid material/non-finite occurrences were `0 / 0`, and exact reset equivalence was `true`.

The human acceptance does not rewrite the automatic verdict to `PASS`. Known follow-up is limited to the top seam being the only through opening, a small persistent vent plume, and broad terminal Pressure activity. No production-physics defect is established. G8-C will measure the tail/activity workload cost rather than treating the review-only tail as a Scenario 4 acceptance blocker.

## Authored 256 x 256 x 64 fixture

All rectangles below are half-open.

| Region | Coordinates | Tick-0 state |
|---|---|---|
| Stone chamber shell | `[32,224) x [38,224)` | Stone shell/base |
| Filled cavity | `[40,216) x [46,216)` | Water, T=110, P=180 |
| Steam core | `[52,204) x [58,132)` | Steam replaces Water, T=110 |
| Low-pressure band | `[112,144) x [80,190)` | P=20 replaces P=180 |
| Top relief seam | `[104,152) x [38,46)` | Wood, T=0 after causal remediation |
| Bottom relief seam | `[116,140) x [216,224)` | Wood, T=0 |
| Left reinforcement | `[24,32) x [116,148)` | Stone |
| Right reinforcement | `[224,232) x [116,148)` | Stone |

Exact tick-0 counts remain Water `18,672`, Steam `11,248`, Wood `576` (`384` top + `192` bottom), Stone `5,728`, Boundary `1,020`, and Empty `28,292`. Temperature counts after the single remediation are T=110 `29,920`, T=95 `0`, and T=0 `35,616`. Pressure counts remain P=180 `26,400`, P=20 `3,520`, and P=0 `35,616`. The fixed cavity mean/max Pressure remains `161.1764705882 / 180`. All flags, seam fuel progress, and 16 chunk edit-wake values are zero.

The cavity has no authored Empty cells. The two eight-cell-thick Wood seams are the only breakable paths from the cavity to the exterior. Their inner faces touch Water at P=180 and their outer faces touch Empty.

## Expected production causal chain

The evidence target is:

`hot pressurized Water/Steam chamber -> Pressure propagation -> Wood relief-seam stress -> rupture -> persistent opening -> Steam/gas vent to exterior -> chamber Pressure relief`

Production pass order means rupture occurs after the current tick's movement; vent movement can begin on later ticks. Pressure is spatial rather than Matter-owned, and vacated Empty cells are cleared by the production pressure path.

Both seam inner faces already touch P=180, above the Wood rupture threshold, so early damage milestones may share tick 1. After causal remediation both seams begin at T=0, below the Wood ignition threshold T=90, with authored flags and fuel progress zero. Top Wood loss or Smoke alone is still insufficient rupture attribution; the Harness separately records subsystem-specific seam combustion/flame/fuel, opening persistence, outside-chamber Steam, and chamber Pressure response.

## Measurement boundary

- Chamber Pressure uses the fixed cavity mask `[40,216) x [46,216)`, including zero-pressure cells after evacuation.
- Outside-chamber Steam means cells outside `[32,224) x [38,224)`; Steam inside a seam volume is not yet exterior venting.
- Raw `relief_seam_open_cells` is the number of non-Wood cells in the authored seam and remains damage/caption telemetry only. It does not establish topology.
- A through-open lane is one authored seam x-column whose complete eight-cell thickness is made only of Empty, Steam, or Smoke. Water, Wood, Stone, Boundary, Sand, Oil, Ice, and invalid material IDs are not passable for this predicate.
- Rupture, persistent-opening streak/confirmation, per-seam opening review flags, reseal, and the post-opening timer use `relief_seam_through_open_lanes`, including its top/bottom components. Tick-1 raw damage does not start a streak when the through-lane count is zero.
- A confirmed opening that later returns to zero through-open lanes is an explicit hard failure and receives an `opening-reseal` evidence badge. Raw non-Wood cells may remain nonzero during that reseal finding.
- Causal vent evidence requires persistent-opening confirmation followed by Steam in an authored seam and exterior Steam at the same or a later diagnostic sample. The Harness does not invent sub-sample ordering when both observations first occur together.
- `pressure_active_cells` measures the production activity mask and is not a substitute for absolute chamber Pressure.
- Chamber mean/max values are canonicalized to the same nine-decimal values written to telemetry before peak, relief, and runaway decisions; the independent coordinator rejects a one-unit (`1e-9`) recorded drift.
- Pressure need not reach zero and the whole world need not reach all-sleep.
- Long Pressure/Thermal tails, remaining terminal activity, a persistent plume, or only one ruptured seam are human-review conditions, not automatic failure by themselves. Runnable chunks alone do not impose an all-sleep requirement.

## Evidence and publication contract

Pressure uses the existing external no-overwrite Harness, frozen executable, receipt-last Run directory, and candidate-only sibling Audit Bundle. Contact Sheet output is chronological by simulation tick; same-tick/same-state milestones are folded into one tile with multiple badges and reset remains last.

Starting with this candidate, the Audit Bundle distinguishes exact build-input bytes from the canonical Git archive. `SOURCE_INPUT_BYTES.zip` preserves the bytes named by `SOURCE_INPUT_MANIFEST.json`; `GIT_SOURCE_ARCHIVE.zip` preserves the commit-addressable Git view. `AUDIT_BUNDLE_MANIFEST.json` records direct members, nested Review Packet inventory, original-path mapping, omissions, and verification scopes. `AUDIT_BUNDLE_HASHES.sha256` authenticates the bundle-local inventory. The sibling ZIP/sidecar remain post-Receipt delivery artifacts and do not change receipt-last semantics inside the Run directory.

## Validation state

- exact fixture pin: passed (`1` test);
- Pressure Rust analyzer/lifecycle/frame/precision/through-lane tests: passed (`16` tests);
- Pressure CLI and legacy lifecycle-isolation tests: passed;
- Python Pressure coordinator tests: passed (`7` tests);
- complete Sand/Water/Fire/Pressure coordinator regression: passed (`48` tests);
- all-six shared scenario GPU reset/regression suite: passed (`3` tests);
- `cargo fmt --all -- --check`: passed;
- `cargo check --workspace --all-targets`: passed;
- changed-package all-target Clippy with warnings denied: passed;
- tracked and new-file whitespace checks: passed;
- `tools/dev.ps1 audit -Strict`: passed with only the two accepted G7/G8 legacy-launcher migration warnings;
- `tools/dev.ps1 validation-plan -BaseRef 941a888c1d7398a9bb1423ecc26a2acd626f7177`: `FULL recommended`, not required; smoke and candidate require review;
- the immutable scratch exposed a concrete topology mismatch: one-layer non-Wood damage was misclassified as a persistent opening;
- Rust/Python through-lane exact-contract remediation and regression validation: passed (`16` Rust Pressure tests, `7` Python Pressure tests, and `48` complete Python coordinator tests);
- workspace FULL: not run (`0` executions), as the plan did not return `FULL required`.
- final causal-remediation validation: timer tests `7 / 7`, Pressure Rust tests `20 / 20`, Python coordinator regression `55 / 55`, fixture/GPU-reset/experiment regressions, fmt/check/clippy/audit/diff-check all passed;
- clean source `43e19d0f3b43aa0c15bf31e98f6401ba5f885170`: one bounded Gallery smoke and exactly one Pressure candidate completed; Pressure candidate reruns for this acceptance closure: `0`.

The validation-plan output counted Git LF/CRLF advisory text as additional unknown paths and broad first-match rules classified the coordinator and CLI under app-readback/provenance. This is a policy-tooling follow-up; it did not change the result from recommended to required and is not modified in this scenario task.

## Acceptance closure and next scenario

- The causal-remediation scratch execution count is `1`; it is exhausted with causal classification `pressure_opening_precedes_combustion`, all hard predicates passing, and `candidate_blocker=false`.
- Production physics, fixture geometry, authored Pressure, rupture thresholds, and shared staging/reset remain unchanged; only the authored top-seam temperature was removed.
- The immutable clean-source candidate is `g8b-pressure-burst-v0-20260818T101046792957Z-17158748` at source `43e19d0f3b43aa0c15bf31e98f6401ba5f885170`; neither it nor its Receipt, Review Packet, Audit Bundle, or other artifacts are modified or regenerated by this docs-only closure.
- Pressure Burst is **USER ACCEPTED WITH KNOWN FOLLOW-UP** while automatic `NEEDS_HUMAN_REVIEW` remains unchanged.
- Scenarios 1–4 are user accepted. Scenario 5 Heavy Mixed World is **NEXT**, so G8-B remains **NOT CLOSED**; G8-C remains a separate later gate.
